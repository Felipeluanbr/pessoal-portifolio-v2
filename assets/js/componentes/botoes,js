document.addEventListener("DOMContentLoaded", () => {
  fetch("../../componentes/botoes.html")
    .then(response => response.text())
    .then(html => {
      document.getElementById("botoes").innerHTML = html;
    })
    .catch(err => console.error("Erro ao carregar o navbar:", err));
});
